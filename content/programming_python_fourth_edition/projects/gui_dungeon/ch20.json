{
  "step_id": "pp_dng_ch20",
  "book_id": "programming_python_fourth_edition",
  "chapter_num": 20,
  "title": "Combat System — Turn-Based Battles",
  "description": "Create Enemy, CombatManager, and CombatLog classes for a turn-based combat system. Enemies have attack power and a loot table. CombatManager handles the flow of battle: starting combat, player attacks, enemy attacks, and checking if combat is over. CombatLog formats and stores combat messages.",
  "builds_on": "pp_dng_ch11",
  "starter_code": "# GUI Dungeon Crawler — Step 7: Combat System\n# Turn-based combat with enemies.\n\nimport tkinter as tk\nimport random\n\n\nclass GameWindow:\n    \"\"\"Main game window with canvas and status label.\"\"\"\n\n    def __init__(self, title='Dungeon Crawler', width=800, height=600):\n        self.width = width\n        self.height = height\n        self.root = tk.Tk()\n        self.root.title(title)\n        self.root.resizable(False, False)\n        self.configure_grid()\n        self.canvas = tk.Canvas(self.root, width=width - 200, height=height - 40, bg='black')\n        self.canvas.grid(row=0, column=0, sticky='nsew')\n        self.status_label = tk.Label(\n            self.root, text='Welcome to the Dungeon!',\n            fg='white', bg='#333333', anchor='w', padx=10\n        )\n        self.status_label.grid(row=1, column=0, columnspan=2, sticky='ew')\n\n    def configure_grid(self):\n        self.root.rowconfigure(0, weight=1)\n        self.root.rowconfigure(1, weight=0)\n        self.root.columnconfigure(0, weight=1)\n        self.root.columnconfigure(1, weight=0)\n\n    def set_status(self, text):\n        self.status_label.config(text=text)\n\n    def run(self):\n        self.root.mainloop()\n\n\nclass TileMap:\n    \"\"\"Draws a tile-based map on a tkinter Canvas.\"\"\"\n\n    TILE_SIZE = 32\n    tile_colors = {\n        'floor': '#808080', 'wall': '#404040',\n        'door': '#8B4513', 'treasure': '#FFD700', 'enemy': '#FF4444',\n    }\n\n    def draw_map(self, canvas, map_data):\n        canvas.delete('all')\n        for row_idx, row in enumerate(map_data):\n            for col_idx, tile in enumerate(row):\n                x = col_idx * self.TILE_SIZE\n                y = row_idx * self.TILE_SIZE\n                color = self.tile_colors.get(tile, 'black')\n                canvas.create_rectangle(\n                    x, y, x + self.TILE_SIZE, y + self.TILE_SIZE,\n                    fill=color, outline=''\n                )\n\n\nclass DungeonGenerator:\n    \"\"\"Generates a random dungeon as a 2D grid.\"\"\"\n\n    def generate(self, width, height, num_rooms=5, seed=None):\n        if seed is not None:\n            random.seed(seed)\n        grid = [['wall'] * width for _ in range(height)]\n        rooms = self.place_rooms(grid, width, height, num_rooms)\n        self.connect_rooms(grid, rooms)\n        self.place_items(grid)\n        self.place_enemies(grid)\n        return grid\n\n    def place_rooms(self, grid, width, height, num_rooms):\n        rooms = []\n        for _ in range(num_rooms):\n            rw, rh = random.randint(3, 8), random.randint(3, 8)\n            rx = random.randint(1, width - rw - 1)\n            ry = random.randint(1, height - rh - 1)\n            for y in range(ry, ry + rh):\n                for x in range(rx, rx + rw):\n                    grid[y][x] = 'floor'\n            rooms.append({'x': rx, 'y': ry, 'w': rw, 'h': rh})\n        return rooms\n\n    def connect_rooms(self, grid, rooms):\n        for i in range(len(rooms) - 1):\n            cx1 = rooms[i]['x'] + rooms[i]['w'] // 2\n            cy1 = rooms[i]['y'] + rooms[i]['h'] // 2\n            cx2 = rooms[i + 1]['x'] + rooms[i + 1]['w'] // 2\n            cy2 = rooms[i + 1]['y'] + rooms[i + 1]['h'] // 2\n            for x in range(min(cx1, cx2), max(cx1, cx2) + 1):\n                grid[cy1][x] = 'floor'\n            for y in range(min(cy1, cy2), max(cy1, cy2) + 1):\n                grid[y][cx2] = 'floor'\n\n    def place_items(self, grid):\n        for row in grid:\n            for i, tile in enumerate(row):\n                if tile == 'floor' and random.random() < 0.05:\n                    row[i] = 'treasure'\n\n    def place_enemies(self, grid):\n        for row in grid:\n            for i, tile in enumerate(row):\n                if tile == 'floor' and random.random() < 0.03:\n                    row[i] = 'enemy'\n\n\nclass Entity:\n    \"\"\"Base class for all game entities.\"\"\"\n\n    def __init__(self, x, y, name, symbol, health=100):\n        self.x = x\n        self.y = y\n        self.name = name\n        self.symbol = symbol\n        self.health = health\n\n\nclass Player(Entity):\n    \"\"\"The player character.\"\"\"\n\n    def __init__(self, x, y):\n        super().__init__(x, y, 'Player', '@', 100)\n        self.inventory = []\n        self.gold = 0\n        self.attack_power = 15\n\n    def move(self, dx, dy, game_map):\n        new_x = self.x + dx\n        new_y = self.y + dy\n        if 0 <= new_y < len(game_map) and 0 <= new_x < len(game_map[0]):\n            if game_map[new_y][new_x] != 'wall':\n                self.x = new_x\n                self.y = new_y\n                return True\n        return False\n\n\ndef draw_entity(canvas, entity, tile_size=32):\n    \"\"\"Draw an entity on the canvas as a colored oval.\"\"\"\n    x = entity.x * tile_size + 4\n    y = entity.y * tile_size + 4\n    color = '#4488FF' if entity.symbol == '@' else '#FF4444'\n    canvas.create_oval(x, y, x + tile_size - 8, y + tile_size - 8, fill=color)\n    canvas.create_text(\n        entity.x * tile_size + tile_size // 2,\n        entity.y * tile_size + tile_size // 2,\n        text=entity.symbol, fill='white', font=('Arial', 14, 'bold')\n    )\n\n\nclass InventoryPanel(tk.Frame):\n    \"\"\"Side panel displaying player inventory and stats.\"\"\"\n\n    def __init__(self, parent, **kwargs):\n        super().__init__(parent, **kwargs)\n        self.stats_label = tk.Label(self, text='HP: -- | Gold: --',\n                                    fg='white', bg='#222222', anchor='w', padx=5, pady=5)\n        self.stats_label.pack(fill='x')\n        tk.Label(self, text='Inventory:', fg='#AAAAAA', bg='#222222',\n                 anchor='w', padx=5).pack(fill='x')\n        self.items_listbox = tk.Listbox(self, bg='#333333', fg='white',\n                                         selectbackground='#555555')\n        self.items_listbox.pack(expand=True, fill='both', padx=5, pady=5)\n        btn_frame = tk.Frame(self, bg='#222222')\n        btn_frame.pack(fill='x', padx=5, pady=5)\n        self.use_btn = tk.Button(btn_frame, text='Use', width=8)\n        self.use_btn.pack(side='left', padx=2)\n        self.drop_btn = tk.Button(btn_frame, text='Drop', width=8)\n        self.drop_btn.pack(side='left', padx=2)\n\n    def update_display(self, player):\n        self.stats_label.config(text=f'HP: {player.health} | Gold: {player.gold}')\n        self.items_listbox.delete(0, tk.END)\n        for item in player.inventory:\n            self.items_listbox.insert(tk.END, item)\n\n\nclass GameLoop:\n    \"\"\"Manages the game loop using tkinter after().\"\"\"\n\n    def __init__(self, root, canvas, fps=10):\n        self.root = root\n        self.canvas = canvas\n        self.fps = fps\n        self.running = False\n        self.game_state = {}\n        self.keys_pressed = set()\n        canvas.bind('<KeyPress>', self._on_key_press)\n        canvas.bind('<KeyRelease>', self._on_key_release)\n        canvas.focus_set()\n\n    def start(self):\n        self.running = True\n        self.tick()\n\n    def stop(self):\n        self.running = False\n\n    def tick(self):\n        if not self.running:\n            return\n        self.update()\n        self.render()\n        self.root.after(1000 // self.fps, self.tick)\n\n    def update(self):\n        player = self.game_state.get('player')\n        game_map = self.game_state.get('map')\n        if player and game_map:\n            if 'w' in self.keys_pressed or 'Up' in self.keys_pressed:\n                player.move(0, -1, game_map)\n            if 's' in self.keys_pressed or 'Down' in self.keys_pressed:\n                player.move(0, 1, game_map)\n            if 'a' in self.keys_pressed or 'Left' in self.keys_pressed:\n                player.move(-1, 0, game_map)\n            if 'd' in self.keys_pressed or 'Right' in self.keys_pressed:\n                player.move(1, 0, game_map)\n            self.keys_pressed.clear()\n\n    def render(self):\n        pass\n\n    def _on_key_press(self, event):\n        self.keys_pressed.add(event.keysym)\n\n    def _on_key_release(self, event):\n        self.keys_pressed.discard(event.keysym)\n\n\nclass Enemy(Entity):\n    \"\"\"An enemy entity with attack power and loot.\n\n    Attributes:\n        attack_power: Damage dealt per attack.\n        loot_table: List of item name strings the enemy can drop.\n    \"\"\"\n\n    def __init__(self, x, y, name='Goblin', symbol='G', health=50,\n                 attack_power=10, loot_table=None):\n        \"\"\"Initialize an enemy.\n\n        Args:\n            x: Column position.\n            y: Row position.\n            name: Enemy name.\n            symbol: Display symbol.\n            health: Starting health.\n            attack_power: Damage per attack.\n            loot_table: List of possible loot item names.\n        \"\"\"\n        # Your code here:\n        # 1. Call super().__init__ with x, y, name, symbol, health\n        # 2. Store attack_power\n        # 3. Store loot_table (default to ['Gold Coin'] if None)\n        pass\n\n\nclass CombatLog:\n    \"\"\"Stores and formats combat messages.\n\n    Attributes:\n        messages: List of combat message strings.\n    \"\"\"\n\n    def __init__(self):\n        # Your code here:\n        # Initialize self.messages as an empty list\n        pass\n\n    def add(self, message):\n        \"\"\"Add a message to the combat log.\"\"\"\n        # Your code here\n        pass\n\n    def get_messages(self):\n        \"\"\"Return all messages as a list of strings.\"\"\"\n        # Your code here\n        return []\n\n    def clear(self):\n        \"\"\"Clear all messages.\"\"\"\n        # Your code here\n        pass\n\n\nclass CombatManager:\n    \"\"\"Manages turn-based combat between player and enemy.\n\n    Attributes:\n        player: The Player instance.\n        enemy: The Enemy instance.\n        log: A CombatLog for recording combat events.\n        active: Whether combat is currently in progress.\n    \"\"\"\n\n    def __init__(self):\n        # Your code here:\n        # Initialize player, enemy as None\n        # Initialize log as a CombatLog\n        # Initialize active as False\n        pass\n\n    def start_combat(self, player, enemy):\n        \"\"\"Begin combat between player and enemy.\n\n        Args:\n            player: The Player instance.\n            enemy: The Enemy instance.\n        \"\"\"\n        # Your code here:\n        # 1. Store player and enemy\n        # 2. Set active = True\n        # 3. Clear the log\n        # 4. Log a message like 'Combat started: Player vs Goblin!'\n        pass\n\n    def player_attack(self):\n        \"\"\"Player attacks the enemy.\n\n        Deals player.attack_power +/- random variance (0-5) damage.\n        Logs the attack and damage dealt.\n        \"\"\"\n        # Your code here:\n        # 1. Calculate damage = player.attack_power + random.randint(-5, 5)\n        # 2. Ensure damage >= 0\n        # 3. Subtract damage from enemy.health\n        # 4. Log message like 'Player hits Goblin for 12 damage!'\n        # 5. If enemy.health <= 0, log 'Goblin is defeated!' and set active = False\n        pass\n\n    def enemy_attack(self):\n        \"\"\"Enemy attacks the player.\n\n        Deals enemy.attack_power +/- random variance (0-3) damage.\n        Logs the attack and damage dealt.\n        \"\"\"\n        # Your code here:\n        # Similar to player_attack but enemy attacks player\n        pass\n\n    def is_combat_over(self):\n        \"\"\"Check if combat has ended.\n\n        Returns:\n            True if combat is over (enemy or player health <= 0).\n        \"\"\"\n        # Your code here\n        return True\n\n\n# Test it\nif __name__ == '__main__':\n    player = Player(0, 0)\n    player.attack_power = 15\n    enemy = Enemy(1, 1, name='Skeleton', health=40, attack_power=8)\n    cm = CombatManager()\n    cm.start_combat(player, enemy)\n    round_num = 0\n    while not cm.is_combat_over():\n        round_num += 1\n        print(f'--- Round {round_num} ---')\n        cm.player_attack()\n        if not cm.is_combat_over():\n            cm.enemy_attack()\n    print('\\nCombat Log:')\n    for msg in cm.log.get_messages():\n        print(f'  {msg}')\n    print(f'\\nPlayer HP: {player.health}, Enemy HP: {enemy.health}')\n",
  "test_code": "e = Enemy(3, 4, name='Rat', symbol='R', health=30, attack_power=5, loot_table=['Cheese'])\nassert e.x == 3, 'Enemy x should be 3'\nassert e.y == 4, 'Enemy y should be 4'\nassert e.name == 'Rat', \"Enemy name should be 'Rat'\"\nassert e.health == 30, 'Enemy health should be 30'\nassert e.attack_power == 5, 'Enemy attack_power should be 5'\nassert e.loot_table == ['Cheese'], \"Enemy loot_table should be ['Cheese']\"\ne2 = Enemy(0, 0)\nassert e2.loot_table == ['Gold Coin'], 'Default loot_table should be [\"Gold Coin\"]'\ncl = CombatLog()\nassert hasattr(cl, 'messages'), 'CombatLog must have messages attribute'\ncl.add('Test message')\nassert len(cl.get_messages()) == 1, 'Should have 1 message'\nassert cl.get_messages()[0] == 'Test message', 'Message should match'\ncl.clear()\nassert len(cl.get_messages()) == 0, 'Should be empty after clear'\ncm = CombatManager()\nassert hasattr(cm, 'log'), 'CombatManager must have log attribute'\nassert isinstance(cm.log, CombatLog), 'log must be a CombatLog instance'\nassert cm.active == False, 'active should start False'\np = Player(0, 0)\np.attack_power = 20\nen = Enemy(1, 1, health=30, attack_power=5)\ncm.start_combat(p, en)\nassert cm.active == True, 'active should be True after start_combat'\nold_health = en.health\ncm.player_attack()\nassert en.health < old_health, 'player_attack should reduce enemy health'",
  "acceptance_criteria": [
    "Enemy inherits from Entity with attack_power and loot_table attributes",
    "Default loot_table is ['Gold Coin'] when None is passed",
    "CombatLog stores messages, supports add/get_messages/clear",
    "CombatManager has start_combat, player_attack, enemy_attack, is_combat_over methods",
    "start_combat sets active to True, stores player/enemy, clears and logs start message",
    "player_attack reduces enemy health with randomized damage and logs the result",
    "enemy_attack reduces player health with randomized damage and logs the result",
    "is_combat_over returns True when either combatant has health <= 0"
  ],
  "hints": [
    "Enemy.__init__: call super().__init__(x, y, name, symbol, health)",
    "Default loot: self.loot_table = loot_table if loot_table is not None else ['Gold Coin']",
    "Damage variance: damage = max(0, attack_power + random.randint(-5, 5))",
    "Combat over: return self.enemy.health <= 0 or self.player.health <= 0 or not self.active"
  ]
}
