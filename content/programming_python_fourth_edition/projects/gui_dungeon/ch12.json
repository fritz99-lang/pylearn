{
  "step_id": "pp_dng_ch12",
  "book_id": "programming_python_fourth_edition",
  "chapter_num": 12,
  "title": "Combat Dialog — Battle UI",
  "description": "Create a CombatDialog class as a Toplevel window that shows the battle UI. It displays player vs enemy health bars, attack/flee/use item buttons, and a scrolling Text widget for the combat log. The show_combat method opens the dialog and starts a combat encounter.",
  "builds_on": "pp_dng_ch20",
  "starter_code": "# GUI Dungeon Crawler — Step 8: Combat Dialog\n# Toplevel battle window with health bars and combat log.\n\nimport tkinter as tk\nimport random\n\n\nclass GameWindow:\n    \"\"\"Main game window with canvas and status label.\"\"\"\n\n    def __init__(self, title='Dungeon Crawler', width=800, height=600):\n        self.width = width\n        self.height = height\n        self.root = tk.Tk()\n        self.root.title(title)\n        self.root.resizable(False, False)\n        self.configure_grid()\n        self.canvas = tk.Canvas(self.root, width=width - 200, height=height - 40, bg='black')\n        self.canvas.grid(row=0, column=0, sticky='nsew')\n        self.status_label = tk.Label(\n            self.root, text='Welcome to the Dungeon!',\n            fg='white', bg='#333333', anchor='w', padx=10\n        )\n        self.status_label.grid(row=1, column=0, columnspan=2, sticky='ew')\n\n    def configure_grid(self):\n        self.root.rowconfigure(0, weight=1)\n        self.root.rowconfigure(1, weight=0)\n        self.root.columnconfigure(0, weight=1)\n        self.root.columnconfigure(1, weight=0)\n\n    def set_status(self, text):\n        self.status_label.config(text=text)\n\n    def run(self):\n        self.root.mainloop()\n\n\nclass TileMap:\n    \"\"\"Draws a tile-based map on a tkinter Canvas.\"\"\"\n\n    TILE_SIZE = 32\n    tile_colors = {\n        'floor': '#808080', 'wall': '#404040',\n        'door': '#8B4513', 'treasure': '#FFD700', 'enemy': '#FF4444',\n    }\n\n    def draw_map(self, canvas, map_data):\n        canvas.delete('all')\n        for row_idx, row in enumerate(map_data):\n            for col_idx, tile in enumerate(row):\n                x = col_idx * self.TILE_SIZE\n                y = row_idx * self.TILE_SIZE\n                color = self.tile_colors.get(tile, 'black')\n                canvas.create_rectangle(\n                    x, y, x + self.TILE_SIZE, y + self.TILE_SIZE,\n                    fill=color, outline=''\n                )\n\n\nclass DungeonGenerator:\n    \"\"\"Generates a random dungeon as a 2D grid.\"\"\"\n\n    def generate(self, width, height, num_rooms=5, seed=None):\n        if seed is not None:\n            random.seed(seed)\n        grid = [['wall'] * width for _ in range(height)]\n        rooms = self.place_rooms(grid, width, height, num_rooms)\n        self.connect_rooms(grid, rooms)\n        self.place_items(grid)\n        self.place_enemies(grid)\n        return grid\n\n    def place_rooms(self, grid, width, height, num_rooms):\n        rooms = []\n        for _ in range(num_rooms):\n            rw, rh = random.randint(3, 8), random.randint(3, 8)\n            rx = random.randint(1, width - rw - 1)\n            ry = random.randint(1, height - rh - 1)\n            for y in range(ry, ry + rh):\n                for x in range(rx, rx + rw):\n                    grid[y][x] = 'floor'\n            rooms.append({'x': rx, 'y': ry, 'w': rw, 'h': rh})\n        return rooms\n\n    def connect_rooms(self, grid, rooms):\n        for i in range(len(rooms) - 1):\n            cx1 = rooms[i]['x'] + rooms[i]['w'] // 2\n            cy1 = rooms[i]['y'] + rooms[i]['h'] // 2\n            cx2 = rooms[i + 1]['x'] + rooms[i + 1]['w'] // 2\n            cy2 = rooms[i + 1]['y'] + rooms[i + 1]['h'] // 2\n            for x in range(min(cx1, cx2), max(cx1, cx2) + 1):\n                grid[cy1][x] = 'floor'\n            for y in range(min(cy1, cy2), max(cy1, cy2) + 1):\n                grid[y][cx2] = 'floor'\n\n    def place_items(self, grid):\n        for row in grid:\n            for i, tile in enumerate(row):\n                if tile == 'floor' and random.random() < 0.05:\n                    row[i] = 'treasure'\n\n    def place_enemies(self, grid):\n        for row in grid:\n            for i, tile in enumerate(row):\n                if tile == 'floor' and random.random() < 0.03:\n                    row[i] = 'enemy'\n\n\nclass Entity:\n    \"\"\"Base class for all game entities.\"\"\"\n\n    def __init__(self, x, y, name, symbol, health=100):\n        self.x = x\n        self.y = y\n        self.name = name\n        self.symbol = symbol\n        self.health = health\n\n\nclass Player(Entity):\n    \"\"\"The player character.\"\"\"\n\n    def __init__(self, x, y):\n        super().__init__(x, y, 'Player', '@', 100)\n        self.inventory = []\n        self.gold = 0\n        self.attack_power = 15\n\n    def move(self, dx, dy, game_map):\n        new_x = self.x + dx\n        new_y = self.y + dy\n        if 0 <= new_y < len(game_map) and 0 <= new_x < len(game_map[0]):\n            if game_map[new_y][new_x] != 'wall':\n                self.x = new_x\n                self.y = new_y\n                return True\n        return False\n\n\ndef draw_entity(canvas, entity, tile_size=32):\n    \"\"\"Draw an entity on the canvas as a colored oval.\"\"\"\n    x = entity.x * tile_size + 4\n    y = entity.y * tile_size + 4\n    color = '#4488FF' if entity.symbol == '@' else '#FF4444'\n    canvas.create_oval(x, y, x + tile_size - 8, y + tile_size - 8, fill=color)\n    canvas.create_text(\n        entity.x * tile_size + tile_size // 2,\n        entity.y * tile_size + tile_size // 2,\n        text=entity.symbol, fill='white', font=('Arial', 14, 'bold')\n    )\n\n\nclass InventoryPanel(tk.Frame):\n    \"\"\"Side panel displaying player inventory and stats.\"\"\"\n\n    def __init__(self, parent, **kwargs):\n        super().__init__(parent, **kwargs)\n        self.stats_label = tk.Label(self, text='HP: -- | Gold: --',\n                                    fg='white', bg='#222222', anchor='w', padx=5, pady=5)\n        self.stats_label.pack(fill='x')\n        tk.Label(self, text='Inventory:', fg='#AAAAAA', bg='#222222',\n                 anchor='w', padx=5).pack(fill='x')\n        self.items_listbox = tk.Listbox(self, bg='#333333', fg='white',\n                                         selectbackground='#555555')\n        self.items_listbox.pack(expand=True, fill='both', padx=5, pady=5)\n        btn_frame = tk.Frame(self, bg='#222222')\n        btn_frame.pack(fill='x', padx=5, pady=5)\n        self.use_btn = tk.Button(btn_frame, text='Use', width=8)\n        self.use_btn.pack(side='left', padx=2)\n        self.drop_btn = tk.Button(btn_frame, text='Drop', width=8)\n        self.drop_btn.pack(side='left', padx=2)\n\n    def update_display(self, player):\n        self.stats_label.config(text=f'HP: {player.health} | Gold: {player.gold}')\n        self.items_listbox.delete(0, tk.END)\n        for item in player.inventory:\n            self.items_listbox.insert(tk.END, item)\n\n\nclass GameLoop:\n    \"\"\"Manages the game loop using tkinter after().\"\"\"\n\n    def __init__(self, root, canvas, fps=10):\n        self.root = root\n        self.canvas = canvas\n        self.fps = fps\n        self.running = False\n        self.game_state = {}\n        self.keys_pressed = set()\n        canvas.bind('<KeyPress>', self._on_key_press)\n        canvas.bind('<KeyRelease>', self._on_key_release)\n        canvas.focus_set()\n\n    def start(self):\n        self.running = True\n        self.tick()\n\n    def stop(self):\n        self.running = False\n\n    def tick(self):\n        if not self.running:\n            return\n        self.update()\n        self.render()\n        self.root.after(1000 // self.fps, self.tick)\n\n    def update(self):\n        player = self.game_state.get('player')\n        game_map = self.game_state.get('map')\n        if player and game_map:\n            if 'w' in self.keys_pressed or 'Up' in self.keys_pressed:\n                player.move(0, -1, game_map)\n            if 's' in self.keys_pressed or 'Down' in self.keys_pressed:\n                player.move(0, 1, game_map)\n            if 'a' in self.keys_pressed or 'Left' in self.keys_pressed:\n                player.move(-1, 0, game_map)\n            if 'd' in self.keys_pressed or 'Right' in self.keys_pressed:\n                player.move(1, 0, game_map)\n            self.keys_pressed.clear()\n\n    def render(self):\n        pass\n\n    def _on_key_press(self, event):\n        self.keys_pressed.add(event.keysym)\n\n    def _on_key_release(self, event):\n        self.keys_pressed.discard(event.keysym)\n\n\nclass Enemy(Entity):\n    \"\"\"An enemy entity with attack power and loot.\"\"\"\n\n    def __init__(self, x, y, name='Goblin', symbol='G', health=50,\n                 attack_power=10, loot_table=None):\n        super().__init__(x, y, name, symbol, health)\n        self.attack_power = attack_power\n        self.loot_table = loot_table if loot_table is not None else ['Gold Coin']\n\n\nclass CombatLog:\n    \"\"\"Stores and formats combat messages.\"\"\"\n\n    def __init__(self):\n        self.messages = []\n\n    def add(self, message):\n        self.messages.append(message)\n\n    def get_messages(self):\n        return list(self.messages)\n\n    def clear(self):\n        self.messages.clear()\n\n\nclass CombatManager:\n    \"\"\"Manages turn-based combat between player and enemy.\"\"\"\n\n    def __init__(self):\n        self.player = None\n        self.enemy = None\n        self.log = CombatLog()\n        self.active = False\n\n    def start_combat(self, player, enemy):\n        self.player = player\n        self.enemy = enemy\n        self.active = True\n        self.log.clear()\n        self.log.add(f'Combat started: {player.name} vs {enemy.name}!')\n\n    def player_attack(self):\n        damage = max(0, self.player.attack_power + random.randint(-5, 5))\n        self.enemy.health -= damage\n        self.log.add(f'{self.player.name} hits {self.enemy.name} for {damage} damage!')\n        if self.enemy.health <= 0:\n            self.enemy.health = 0\n            self.log.add(f'{self.enemy.name} is defeated!')\n            self.active = False\n\n    def enemy_attack(self):\n        damage = max(0, self.enemy.attack_power + random.randint(-3, 3))\n        self.player.health -= damage\n        self.log.add(f'{self.enemy.name} hits {self.player.name} for {damage} damage!')\n        if self.player.health <= 0:\n            self.player.health = 0\n            self.log.add(f'{self.player.name} has fallen!')\n            self.active = False\n\n    def is_combat_over(self):\n        return not self.active or self.player.health <= 0 or self.enemy.health <= 0\n\n\nclass CombatDialog:\n    \"\"\"Toplevel window for the battle UI.\n\n    Attributes:\n        dialog: The Toplevel window.\n        attack_btn: Button to attack the enemy.\n        flee_btn: Button to flee from combat.\n        use_item_btn: Button to use an inventory item.\n        log_text: Text widget displaying the combat log.\n        player_health_label: Label showing player health.\n        enemy_health_label: Label showing enemy health.\n    \"\"\"\n\n    def __init__(self, parent):\n        \"\"\"Initialize the combat dialog (hidden until show_combat is called).\n\n        Args:\n            parent: Parent Tk window.\n        \"\"\"\n        self.parent = parent\n        self.dialog = None\n        self.combat_manager = CombatManager()\n        # These will be created in show_combat:\n        self.attack_btn = None\n        self.flee_btn = None\n        self.use_item_btn = None\n        self.log_text = None\n        self.player_health_label = None\n        self.enemy_health_label = None\n\n    def show_combat(self, player, enemy):\n        \"\"\"Open the combat dialog and start a battle.\n\n        Args:\n            player: The Player instance.\n            enemy: The Enemy instance.\n        \"\"\"\n        # Your code here:\n        # 1. Create self.dialog as a tk.Toplevel(self.parent)\n        # 2. Set title to 'Combat!' and size to 400x350\n        # 3. Make it modal: dialog.grab_set()\n        # 4. Create a health frame at the top with two labels:\n        #    self.player_health_label showing 'Player HP: {health}'\n        #    self.enemy_health_label showing '{enemy.name} HP: {health}'\n        # 5. Create self.log_text — a Text widget (read-only, height=10)\n        #    with dark background and white text\n        # 6. Create a button frame with:\n        #    self.attack_btn — 'Attack' button\n        #    self.flee_btn — 'Flee' button\n        #    self.use_item_btn — 'Use Item' button\n        # 7. Start combat via self.combat_manager.start_combat(player, enemy)\n        # 8. Update the log display\n        pass\n\n    def _update_log(self):\n        \"\"\"Refresh the log_text widget from combat manager's log.\"\"\"\n        # Your code here:\n        # 1. Enable the text widget: self.log_text.config(state='normal')\n        # 2. Clear it: self.log_text.delete('1.0', tk.END)\n        # 3. Insert all messages from self.combat_manager.log.get_messages()\n        # 4. Disable it: self.log_text.config(state='disabled')\n        # 5. Scroll to end: self.log_text.see(tk.END)\n        pass\n\n    def _update_health(self):\n        \"\"\"Update health labels.\"\"\"\n        # Your code here\n        pass\n\n\n# Test it\nif __name__ == '__main__':\n    game = GameWindow()\n    player = Player(0, 0)\n    player.inventory = ['Health Potion']\n    enemy = Enemy(1, 1, name='Skeleton', health=40, attack_power=8)\n    cd = CombatDialog(game.root)\n    cd.show_combat(player, enemy)\n    game.run()\n",
  "test_code": "root = tk.Tk()\nroot.withdraw()\ncd = CombatDialog(root)\nassert hasattr(cd, 'show_combat'), 'CombatDialog must have show_combat method'\nassert callable(cd.show_combat), 'show_combat must be callable'\nassert hasattr(cd, 'combat_manager'), 'CombatDialog must have combat_manager attribute'\np = Player(0, 0)\ne = Enemy(1, 1, name='Rat', health=20, attack_power=3)\ncd.show_combat(p, e)\nassert cd.dialog is not None, 'show_combat should create a dialog'\nassert isinstance(cd.dialog, tk.Toplevel), 'dialog must be a Toplevel'\nassert cd.attack_btn is not None, 'show_combat should create attack_btn'\nassert isinstance(cd.attack_btn, tk.Button), 'attack_btn must be a tk.Button'\nassert cd.flee_btn is not None, 'show_combat should create flee_btn'\nassert isinstance(cd.flee_btn, tk.Button), 'flee_btn must be a tk.Button'\nassert cd.log_text is not None, 'show_combat should create log_text'\nassert isinstance(cd.log_text, tk.Text), 'log_text must be a tk.Text'\nassert cd.combat_manager.active == True, 'Combat should be active after show_combat'\ncd.dialog.destroy()\nroot.destroy()",
  "acceptance_criteria": [
    "CombatDialog creates a Toplevel window when show_combat is called",
    "Dialog shows player and enemy health labels",
    "attack_btn, flee_btn, and use_item_btn are Button widgets",
    "log_text is a read-only Text widget showing combat messages",
    "show_combat starts combat via the internal CombatManager",
    "The dialog is modal (grabs focus)"
  ],
  "hints": [
    "Toplevel: self.dialog = tk.Toplevel(self.parent)",
    "Modal: self.dialog.grab_set()",
    "Read-only text: create with state='disabled', temporarily enable to insert",
    "Health label: tk.Label(frame, text=f'Player HP: {player.health}')",
    "Button: self.attack_btn = tk.Button(btn_frame, text='Attack', command=self._on_attack)"
  ]
}
